# PyChord
A program that is useful for texting by adding a messages program!
How to use = first run Server.exe then PyChord_Client.exe and your friend needs to run PyChord_Client.exe too,to text just login (it saves account existing until you delete it (server.exe)) but first register and then login
and text!
